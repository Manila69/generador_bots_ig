AVISO: Todo el contenido en estos codigos, son con objetivos de mero aprendizaje y practicas de programación: en ningun momento se intenta generar algun daño a Meta.

El codigo de este script todavia esta en desarrollo =)

- Ejecutar estos comandos en el cmd para asegurarnos q funcione correctamente
pip install selenium
pip install webdriver
pip install pyperclip
pip install mimesis
pip install pyautogui
pip install --upgrade webdriver_manager
pip install webdriver_manager==3.5.2
deberian ser todas las dependencias
